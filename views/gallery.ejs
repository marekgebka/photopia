<% include partials/header %>

<h1>This is the Gallery Page</h1>
<a href="/gallery/new">Upload new Image</a>
<% gallery.forEach(function(el){ %>
  <div>
    <h4> <%= el.name %> </h4>
    <img src= '<%= el.image %>' alt= '<%= el.name %>'>
  </div>
 <% }); %>

 <% include partials/footer %>